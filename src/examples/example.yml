description: >
  Example usage to authenticate a gcloud CLI using OIDC,
  assuming the required environment variables are defined in a context.
usage:
  version: 2.1
  orbs:
    gcp: mozilla/gcp@0.0.1
  workflows:
    use-my-orb:
      jobs:
        - gcp/setup
      context: gcp-context
